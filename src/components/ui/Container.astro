---
/**
 * Shared content width for page sections, header, and footer.
 */
interface Props {
	class?: string;
}

const { class: className } = Astro.props;
---

<div class:list={['mx-auto w-full max-w-6xl px-6 md:px-10', className]}>
	<slot />
</div>
